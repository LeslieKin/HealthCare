<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:gravity="center_horizontal"
  android:orientation="vertical" >

  <TextView
    android:id="@+id/text_detect_type"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="@dimen/padding_detect_item"
    android:drawablePadding="@dimen/drawable_padding_detect_item"
    android:drawableTop="@drawable/detect_item_blood_pressure"
    android:gravity="center"
    android:textSize="@dimen/ts_detect_item_title"
    android:textColor="@color/page_main_text"
    android:text="@string/app_name"  />
</LinearLayout>
